<nav class="navbar sticky-top navbar-expand-md navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/changelog">Yelp Camp</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item" id="navbar-home">
            <a class="nav-link active" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item" id="navbar-all-campgrounds">
            <a class="nav-link" href="/campgrounds">Campgrounds</a>
          </li>
          <% if (!(typeof currentUser === 'undefined' || !currentUser)) { %> 
            <li class="nav-item d-none d-lg-inline" id="navbar-my-campgrounds">
            <a class="nav-link" href="/campgrounds/user">My Campgrounds</a>
          </li>
          <% } %>
          <li class="nav-item">
            <a class="nav-link" href="/campgrounds/new">New Campground</a>
          </li>
        </ul>
        <div class="navbar-nav ms-auto">
          <% if (typeof currentUser === 'undefined' || !currentUser) { %> <!-- currentUser kommt aus res.locals.currentUser (und das wird in der middleware aus req.user übernommen ) -->
            <a class="nav-link" href="/login" id="navbar-login">Login</a>
            <a class="nav-link" href="/register" id="navbar-register">Register</a>
          <% } else { %>
            <a class="nav-link" href="/edit" id="navbar-edit-profile">Edit Profile</a>
            <a class="nav-link" href="/logout" id="navbar-logout">Logout <%= currentUser.username %></a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>


  